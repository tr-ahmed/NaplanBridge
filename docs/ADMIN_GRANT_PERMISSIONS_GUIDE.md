# 🔐 دليل صلاحيات المعلم - للأدمن

## 📍 كيف يمنح الأدمن الصلاحيات للمعلم؟

### 🎯 الخطوات:

#### **1️⃣ الذهاب إلى صفحة الصلاحيات**
```
🔗 Route: /admin/teacher-permissions
📍 Navigation: Admin Sidebar → "Teacher Permissions"
🔑 Icon: 🛡️ Shield (fas fa-shield-alt)
```

#### **2️⃣ منح صلاحيات جديدة**
```
1. اضغط على زر "Grant Permission" أو "Add Permission"
2. اختر المعلم من القائمة المنسدلة
3. اختر المادة (Subject)
4. حدد الصلاحيات:
   ✅ Can Create - يستطيع إنشاء محتوى/امتحانات جديدة
   ✅ Can Edit - يستطيع تعديل المحتوى/الامتحانات
   ✅ Can Delete - يستطيع حذف المحتوى/الامتحانات
5. اضغط "Grant" أو "Save"
```

---

## 🔄 تدفق البيانات (Data Flow)

```
┌─────────────────────────────────────────────────────────────┐
│                    ADMIN GRANTS PERMISSION                   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  POST /api/TeacherPermissions/grant                          │
│  {                                                           │
│    teacherId: 5,                                             │
│    subjectId: 3,                                             │
│    canCreate: true,                                          │
│    canEdit: true,                                            │
│    canDelete: false                                          │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│              PERMISSION STORED IN DATABASE                   │
│  Table: TeacherPermissions                                   │
│  - Teacher can now access this subject                       │
│  - Specific CRUD permissions applied                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│           TEACHER SEES THE SUBJECT IN THEIR PANEL            │
│  1. Content Management → Shows only authorized subjects      │
│  2. Exam Management → Shows only authorized exams            │
│  3. Actions limited based on canCreate/canEdit/canDelete     │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 صفحة Teacher Permissions - Features

### **Tab 1: Permissions Management**
```
┌──────────────────────────────────────────────────┐
│  🔍 Search Teachers...                    [+ Grant Permission]  │
├──────────────────────────────────────────────────┤
│                                                  │
│  👨‍🏫 Ahmed Hassan (ahmed@example.com)              │
│  ├─ 📚 Mathematics (Year 5)                      │
│  │   ✅ Create  ✅ Edit  ❌ Delete    [✏️ Edit] [🗑️ Remove]│
│  └─ 🔬 Science (Year 6)                          │
│      ✅ Create  ✅ Edit  ✅ Delete    [✏️ Edit] [🗑️ Remove]│
│                                                  │
│  👩‍🏫 Fatima Ali (fatima@example.com)              │
│  └─ 📖 English (Year 7)                          │
│      ✅ Create  ❌ Edit  ❌ Delete    [✏️ Edit] [🗑️ Remove]│
│                                                  │
└──────────────────────────────────────────────────┘
```

### **Tab 2: Pending Approvals (إذا كان موجود)**
```
┌──────────────────────────────────────────────────┐
│  📋 Content waiting for approval                 │
├──────────────────────────────────────────────────┤
│  👨‍🏫 Teacher: Ahmed Hassan                        │
│  📚 Subject: Mathematics                         │
│  📝 Content: Lesson 5 - Algebra                  │
│  🕐 Submitted: 2 hours ago                       │
│                        [✅ Approve] [❌ Reject]    │
└──────────────────────────────────────────────────┘
```

---

## 🔧 API Endpoints المستخدمة

### **1. Get All Permissions**
```http
GET /api/TeacherPermissions/all
Response: TeacherPermissionDto[]
```

### **2. Grant Permission**
```http
POST /api/TeacherPermissions/grant
Body: {
  teacherId: number,
  subjectId: number,
  canCreate: boolean,
  canEdit: boolean,
  canDelete: boolean,
  notes?: string
}
```

### **3. Update Permission**
```http
PUT /api/TeacherPermissions/{permissionId}
Body: {
  canCreate: boolean,
  canEdit: boolean,
  canDelete: boolean,
  isActive?: boolean
}
```

### **4. Revoke Permission**
```http
POST /api/TeacherPermissions/{permissionId}/revoke
or
DELETE /api/TeacherPermissions/{permissionId}
```

### **5. Get Teacher Permissions**
```http
GET /api/TeacherPermissions/teacher/{teacherId}
Response: TeacherPermissionDto[]
```

---

## 📱 تجربة المستخدم (UX)

### **للأدمن:**
1. يذهب إلى "Teacher Permissions"
2. يرى قائمة بكل المعلمين والصلاحيات الحالية
3. يمكنه:
   - منح صلاحيات جديدة
   - تعديل الصلاحيات الموجودة
   - إلغاء الصلاحيات
   - البحث عن معلم معين

### **للمعلم:**
1. يسجل دخول
2. يذهب إلى "My Exams" أو "Content Management"
3. يرى فقط المواد المصرح له بها
4. يرى الأزرار بناءً على صلاحياته:
   - **Can Create** → يظهر زر "Create"
   - **Can Edit** → تظهر أزرار "Edit"
   - **Can Delete** → تظهر أزرار "Delete"
   - **بدون صلاحيات** → "View Only"

---

## ⚠️ Important Notes

### **للأدمن:**
- ✅ تأكد من منح الصلاحيات قبل أن يبدأ المعلم العمل
- ✅ يمكنك منح صلاحية "Create only" للمعلمين الجدد
- ✅ منح "Edit + Delete" فقط للمعلمين الموثوقين
- ⚠️ إلغاء الصلاحية لا يحذف المحتوى الموجود، فقط يمنع الوصول

### **للمعلم:**
- ℹ️ إذا لم تظهر لك أي مواد، تواصل مع الأدمن
- ℹ️ إذا لا تستطيع إنشاء امتحانات، طلب صلاحية `canCreate`
- ℹ️ الصلاحيات تطبق فوراً بعد منحها

---

## 🔍 Troubleshooting

### **المشكلة: المعلم لا يرى أي مواد**
✅ **الحل:**
1. تحقق من أن الأدمن منح صلاحية على الأقل لمادة واحدة
2. تأكد من أن `isActive = true` في الصلاحية
3. جرب تسجيل خروج ودخول مرة أخرى

### **المشكلة: المعلم لا يستطيع إنشاء امتحانات**
✅ **الحل:**
1. تحقق من أن `canCreate = true` في صلاحياته
2. تأكد من أن الصلاحية على المادة الصحيحة

### **المشكلة: زر Edit/Delete لا يظهر**
✅ **الحل:**
1. تحقق من `canEdit` أو `canDelete` في صلاحيات المادة
2. تأكد من أن المعلم يحاول تعديل امتحان في مادة مصرح له بها

---

## 📋 Checklist للأدمن

عند إضافة معلم جديد:
- [ ] منح صلاحية على المادة/المواد المطلوبة
- [ ] تحديد `canCreate`, `canEdit`, `canDelete` بناءً على الثقة
- [ ] التأكد من أن `isActive = true`
- [ ] إخبار المعلم بالصلاحيات الممنوحة
- [ ] متابعة عمل المعلم في البداية

---

## 🎯 Summary

### كيف يمنح الأدمن الصلاحيات؟

```
1️⃣ Admin Login
    ↓
2️⃣ Navigate to: Admin Sidebar → "Teacher Permissions"
    ↓
3️⃣ Click "Grant Permission" or "+ Add"
    ↓
4️⃣ Select:
   - Teacher (من القائمة)
   - Subject (المادة)
   - Permissions (✅ Create, ✅ Edit, ✅ Delete)
    ↓
5️⃣ Click "Save" or "Grant"
    ↓
6️⃣ ✅ Done! Teacher can now access this subject
```

---

## 📞 مثال عملي

### **السيناريو:**
المعلم "أحمد" يريد إدارة امتحانات مادة **الرياضيات** للصف الخامس

### **خطوات الأدمن:**
```
1. Login as Admin
2. Go to: /admin/teacher-permissions
3. Click "Grant Permission"
4. Select:
   - Teacher: أحمد حسن
   - Subject: Mathematics (Year 5)
   - ✅ Can Create
   - ✅ Can Edit
   - ❌ Can Delete (للحماية)
5. Click "Grant"
```

### **النتيجة:**
```
✅ المعلم "أحمد" يستطيع الآن:
   - رؤية مادة الرياضيات في "My Content"
   - رؤية امتحانات الرياضيات في "My Exams"
   - إنشاء امتحانات جديدة (Create)
   - تعديل الامتحانات (Edit)
   - ❌ لا يستطيع الحذف (Delete not granted)
```

---

## 🔗 الملفات ذات العلاقة

### **Admin Side:**
- `src/app/features/admin/teacher-permissions/teacher-permissions-admin.component.ts`
- `src/app/features/admin/teacher-permissions/teacher-permissions-admin.component.html`
- `src/app/shared/components/admin-sidebar/admin-sidebar.component.html` (القائمة)

### **Teacher Side:**
- `src/app/features/teacher/teacher-exam-management/teacher-exam-management.component.ts`
- `src/app/features/teacher/services/teacher-permission.service.ts`

### **Routes:**
- `/admin/teacher-permissions` - صفحة إدارة الصلاحيات
- `/teacher/exams` - صفحة امتحانات المعلم

---

**✨ الخلاصة:**
الأدمن يمنح الصلاحيات من صفحة **"Teacher Permissions"** الموجودة في القائمة الجانبية للأدمن، ويختار المعلم والمادة والصلاحيات المطلوبة (Create/Edit/Delete).
