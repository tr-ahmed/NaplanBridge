# 📋 ملخص سريع: ايه المطلوب من الباك

**التاريخ:** 18 نوفمبر 2025

---

## 🎯 الحالة الحالية

| الجزء | الحالة |
|-------|---------|
| **Frontend (الواجهة)** | ✅ كامل وجاهز |
| **Backend (الخادم)** | ⚠️ فيه مشاكل |
| **Database (قاعدة البيانات)** | ❓ قد تحتاج عمل |

---

## 🔴 المشاكل الرئيسية

### 1️⃣ الـ Subjects (المواد) مش بتظهر

```
الـ Frontend:
  - يطلب: GET /api/TeacherContent/my-subjects
  - يتوقع: قائمة مواد بتفاصيل كاملة
  
الـ Backend الحالي:
  - يرجع: فقط أرقام [1, 2, 3]
  - ❌ خطأ: ما يرجع التفاصيل والإحصائيات
```

**الحل:** عدّل الـ Endpoint ليرجع:
```json
{
  "subjectId": 1,
  "subjectName": "اسم المادة",
  "yearId": 1,
  "yearName": "السنة",
  "canCreate": true,
  "canEdit": true,
  "canDelete": false,
  "stats": {
    "total": 5,
    "approved": 3,
    "pending": 1,
    "rejected": 0,
    "revisionRequested": 1
  }
}
```

---

### 2️⃣ إنشاء مادة جديدة مش ممكن

```
الـ Frontend:
  - يحاول: POST /api/Subjects
  - يرسل: بيانات المادة + صورة
  
الـ Backend:
  - يرجع: 403 Forbidden
  - ❌ خطأ: فقط Admin
```

**الحل:** اعطِ المعلم الصلاحية أو عدّل الـ Endpoint

---

### 3️⃣ الملفات قد تضيع

```
الـ Frontend:
  - يرسل: multipart/form-data مع صورة وفيديو
  
الـ Backend:
  - ❓ غير واضح: هل يقبل الملفات؟
  - ❓ غير واضح: هل يحفظها بشكل صحيح؟
```

**الحل:** تأكد من الخادم يقبل الملفات ويرجع الـ URLs

---

## 📊 جدول الـ Endpoints المطلوبة

| الـ Endpoint | الـ Method | المطلوب | الأولوية |
|-----------|----------|--------|---------|
| `/api/TeacherContent/my-subjects` | GET | ✅ بيانات كاملة | 🔴 عاجل |
| `/api/Subjects` | POST | ✅ صلاحية أو Endpoint جديد | 🔴 عاجل |
| `/api/TeacherContent/my-content` | GET | ✅ تأكد من الشغل | 🟡 مهم |
| `/api/Lessons` | POST | ✅ ملفات | 🟡 مهم |
| `/api/TeacherContent/pending-approvals` | GET | ✅ تأكد | 🟡 مهم |
| `/api/TeacherContent/approve` | POST | ✅ تأكد | 🟢 بعدين |

---

## 🛠️ الخطوات العملية

### الخطوة 1: المواد (30 دقيقة)

**اللي لازم تعمله:**
1. افتح `TeacherContentController.cs`
2. روح للـ Method `GetMySubjects`
3. عدّله ليرجع بيانات كاملة (اسم المادة، السنة، الإحصائيات)
4. اختبره مع Postman

**الـ Code:**
```csharp
// الشكل الحالي (خطأ):
return Ok(new { data = new[] { 1, 2, 3 } });

// الشكل الصحيح:
return Ok(new { 
  data = new[] {
    new { subjectId = 1, subjectName = "الرياضيات", ... },
    ...
  }
});
```

---

### الخطوة 2: الصلاحيات (15 دقيقة)

**اختر واحد:**

**الخيار أ:** اعطِ الصلاحية (من الـ Database)
```sql
INSERT INTO TeacherPermissions (TeacherId, CanCreate, CanEdit, CanDelete)
VALUES (5, 1, 1, 0);
```

**الخيار ب:** عدّل الـ Code (من الـ Code)
```csharp
// قبل:
[Authorize(Roles = "Admin")]

// بعد:
[Authorize(Roles = "Admin,Teacher")]
```

---

### الخطوة 3: الملفات (1 ساعة)

**اللي لازم تعمله:**
1. تأكد من `LessonsController` يقبل multipart
2. افحص حفظ الملفات
3. تأكد من الـ URLs صحيحة

**الـ Code:**
```csharp
[HttpPost]
public async Task<IActionResult> CreateLesson(
    [FromForm] IFormFile posterFile,
    [FromForm] IFormFile videoFile)
{
    // حفظ الملفات وإرجاع URLs
}
```

---

## ✅ قائمة التحقق

قبل ما تقول "انتهيت"، تأكد من:

- [ ] `/api/TeacherContent/my-subjects` ترجع بيانات كاملة
- [ ] المعلم يقدر ينشئ مادة جديدة
- [ ] الملفات تُحفظ بشكل صحيح
- [ ] `/api/TeacherContent/my-content` بتشتغل
- [ ] الموافقات والرفض بتشتغل
- [ ] الـ Frontend بتشتغل مع الخادم الجديد

---

## 🧪 الاختبار السريع

بعد ما تخلص:

1. **شغّل Frontend:**
   ```bash
   ng serve
   ```

2. **سجّل دخول كمعلم**

3. **روح لـ `/teacher/content-management`**

4. **تأكد:**
   - ✅ شفت المواد الخاصة بك؟
   - ✅ تقدر تنشئ مادة جديدة؟
   - ✅ تقدر تحمّل درس مع فيديو؟
   - ✅ تشوف المحتوى الخاص بك؟

---

## 📞 الأسئلة المهمة

اطرحها على فريق الخادم:

1. **الـ Subjects:**
   - "الـ Endpoint بتاع `/api/TeacherContent/my-subjects` بترجع إيه بالضبط؟"
   - "قدرة نضيف الإحصائيات؟"

2. **الصلاحيات:**
   - "المعلم ممكن ينشئ مادة؟ لو لا، ازي أعطيه الصلاحية؟"

3. **الملفات:**
   - "الخادم يقبل multipart form-data؟"
   - "الملفات بتتحفظ فين؟"
   - "الـ URL بتاع الملف إيه؟"

4. **الموافقات:**
   - "النظام بتاع الموافقة كامل؟"
   - "Admin قادر يوافق على محتوى المعلم؟"

---

## ⏱️ الوقت المتوقع

| المهمة | الوقت |
|-------|-------|
| المواد | 30 دقيقة |
| الصلاحيات | 15 دقيقة |
| الملفات | 1 ساعة |
| الاختبار | 30 دقيقة |
| **الإجمالي** | **~2.5 ساعة** |

---

## 📁 الملفات المهمة

### في الباك (.NET):
- `TeacherContentController.cs` ← **اعدل هنا**
- `SubjectsController.cs` ← **وهنا**
- `LessonsController.cs` ← **وهنا**
- Models و DbContext

### في الفرونت (Angular):
- `teacher-content-management.service.ts` ← جاهز
- `teacher-content-management.component.ts` ← جاهز
- قوالب HTML ← جاهز

---

## 🎯 الهدف النهائي

بعد الانتهاء، المعلم يقدر:

```
✅ يدخل لإدارة المحتوى
✅ يشوف المواد الخاصة به
✅ ينشئ موضوع جديد
✅ ينشئ درس مع فيديو وصورة
✅ يشوف حالة محتواه
✅ يشتغل بسهولة على الموقع
```

---

**جاهز تبدأ؟** 🚀

اقرأ التقارير المفصلة:
1. `reports/backend_inquiries/backend_inquiry_teacher_content_management_2025-11-18.md` (بالإنجليزي)
2. `reports/backend_inquiries/backend_inquiry_teacher_content_AR_2025-11-18.md` (بالعربي)
3. `docs/BACKEND_TEACHER_CONTENT_ACTION_PLAN_AR.md` (خطة العمل مع الـ Code)

