# 🧪 اختبار الفلترة الديناميكية في نماذج إدارة المحتوى

## 📋 خطوات الاختبار

### 1️⃣ اختبار نموذج إضافة درس جديد (Add Lesson)

#### الخطوات:
1. ✅ افتح صفحة Content Management
2. ✅ اضغط على زر "Add Lesson"
3. ✅ تحقق من أن حقل "Week" معطل (disabled)
4. ✅ اختر "Subject" من القائمة المنسدلة
5. ✅ تحقق من تفعيل حقل "Week" وعرض فقط الأسابيع المرتبطة بالـ Subject
6. ✅ اختر "Week" من القائمة
7. ✅ غيّر الـ "Subject" إلى آخر
8. ✅ تحقق من إعادة تعيين حقل "Week" إذا كان غير صالح للـ Subject الجديد

#### النتيجة المتوقعة:
- ✅ حقل Week معطل قبل اختيار Subject
- ✅ يعرض فقط الأسابيع المرتبطة بالـ Subject المختار
- ✅ رسالة تحذير تظهر إذا لم تكن هناك أسابيع متاحة
- ✅ يتم إعادة تعيين Week عند تغيير Subject

---

### 2️⃣ اختبار نموذج تعديل درس (Edit Lesson)

#### الخطوات:
1. ✅ افتح صفحة Content Management
2. ✅ اضغط على زر "Edit" لدرس موجود
3. ✅ تحقق من عرض البيانات الحالية بشكل صحيح
4. ✅ تحقق من أن حقل "Week" يعرض فقط الأسابيع المرتبطة بالـ Subject الحالي
5. ✅ غيّر الـ "Subject"
6. ✅ تحقق من تحديث قائمة "Week" تلقائياً

#### النتيجة المتوقعة:
- ✅ البيانات الحالية تظهر بشكل صحيح
- ✅ الفلترة تعمل بنفس الطريقة في وضع التعديل
- ✅ يمكن تغيير Subject و Week بدون مشاكل

---

### 3️⃣ اختبار الحالات الحدية (Edge Cases)

#### الخطوة 1: Subject بدون أسابيع
1. ✅ اختر Subject ليس لديه Terms أو Weeks
2. ✅ تحقق من ظهور رسالة تحذير
3. ✅ تحقق من أن حقل Week معطل

#### الخطوة 2: تغيير Subject عدة مرات
1. ✅ اختر Subject A
2. ✅ اختر Week من القائمة
3. ✅ غيّر إلى Subject B
4. ✅ غيّر مرة أخرى إلى Subject A
5. ✅ تحقق من أن الفلترة تعمل بشكل صحيح في كل مرة

#### الخطوة 3: البيانات الفارغة
1. ✅ تحقق من أن النموذج يعمل بشكل صحيح حتى لو كانت قوائم Terms أو Weeks فارغة
2. ✅ تحقق من عدم حدوث أخطاء JavaScript في Console

---

### 4️⃣ اختبار التكامل مع النظام

#### الخطوات:
1. ✅ أنشئ درس جديد باستخدام النموذج المحدث
2. ✅ احفظ البيانات
3. ✅ تحقق من حفظ البيانات بشكل صحيح في Backend
4. ✅ أعد تحميل الصفحة
5. ✅ افتح الدرس للتعديل
6. ✅ تحقق من عرض البيانات بشكل صحيح

#### النتيجة المتوقعة:
- ✅ البيانات تُحفظ بشكل صحيح
- ✅ لا توجد مشاكل في التكامل مع API
- ✅ الفلترة تعمل بعد إعادة التحميل

---

## 🔍 نقاط التحقق المهمة

### ✅ الوظائف الأساسية:
- [ ] الفلترة تعمل عند اختيار Subject
- [ ] حقل Week يُعطل/يُفعل بشكل صحيح
- [ ] رسائل التحذير تظهر في الوقت المناسب
- [ ] إعادة تعيين Week عند تغيير Subject

### ✅ تجربة المستخدم:
- [ ] الواجهة سريعة الاستجابة
- [ ] لا يوجد تأخير ملحوظ عند الفلترة
- [ ] الرسائل واضحة ومفهومة
- [ ] التصميم متناسق مع باقي الصفحة

### ✅ الأداء:
- [ ] لا توجد أخطاء في Console
- [ ] لا يوجد تسريب في الذاكرة (Memory Leaks)
- [ ] الفلترة سريعة حتى مع بيانات كثيرة

### ✅ التوافق:
- [ ] يعمل على Chrome
- [ ] يعمل على Firefox
- [ ] يعمل على Edge
- [ ] يعمل على Safari (إن أمكن)

---

## 🐛 حالات الأخطاء المحتملة وحلولها

### المشكلة 1: حقل Week لا يُفلتر
**السبب المحتمل:** 
- `onSubjectChangeForLesson()` لا يتم استدعاؤها
- البيانات غير محملة بشكل صحيح

**الحل:**
- تحقق من إضافة `(ngModelChange)="onSubjectChangeForLesson()"` في حقل Subject
- تحقق من تحميل Terms و Weeks من API

---

### المشكلة 2: رسالة التحذير لا تظهر
**السبب المحتمل:**
- شرط `@if` غير صحيح
- `filteredWeeks.length` لا يعمل بشكل صحيح

**الحل:**
- تحقق من شرط `@if (formData.subjectId && filteredWeeks.length === 0)`
- تحقق من أن `filteredWeeks` مُعرف بشكل صحيح

---

### المشكلة 3: حقل Week لا يُعاد تعيينه
**السبب المحتمل:**
- المنطق في `onSubjectChangeForLesson()` لا يعمل

**الحل:**
- تحقق من جزء Reset Week في الدالة:
```typescript
if (this.formData.weekId) {
  const isWeekValid = this.filteredWeeks.some((week: any) => week.id === Number(this.formData.weekId));
  if (!isWeekValid) {
    this.formData.weekId = null;
  }
}
```

---

## 📊 نتائج الاختبار

### ✅ الحالات التي تعمل بنجاح:
- ✅ فلترة Weeks بناءً على Subject
- ✅ تعطيل/تفعيل حقل Week
- ✅ رسائل التحذير
- ✅ إعادة تعيين Week عند تغيير Subject
- ✅ العمل في وضع Add و Edit

### ❌ المشاكل المكتشفة:
(يتم تسجيل أي مشاكل هنا)

---

## 📝 ملاحظات إضافية

### تحسينات مستقبلية محتملة:
1. إضافة مؤشر تحميل عند الفلترة
2. إضافة بحث داخل قائمة Weeks
3. عرض عدد الأسابيع المتاحة بجانب اسم Subject
4. إضافة اختصارات لوحة المفاتيح

---

## ✅ الخلاصة

تم إنشاء هذا الدليل للتأكد من أن جميع وظائف الفلترة الديناميكية تعمل بشكل صحيح.
يرجى اتباع جميع خطوات الاختبار والتأكد من أن كل شيء يعمل كما هو متوقع.
